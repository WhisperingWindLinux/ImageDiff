# ImageDiff

The application allows you to compare two images pixel by pixel. The user can obtain information about a specific pixel in both images simultaneously, which are displayed in the application window.
